# 2026-05-19 Daily Notes

## Heartbeats / System Checks

### ~01:54 UTC (May 18 evening)
- Heartbeat detected SaaS services DOWN on VPS 203.161.55.37
- Services affected: FlowBridge (3847), MetricPulse (3848), SupportForge (3846)
- PM2 processes were NOT running for these services
- Only gardenproject, lashes, qrforge were running

### ~07:54 UTC - Investigation & Fix
- Found that metricpulse and supporthq directories had only `html/` subdirectory (incomplete install)
- FlowBridge had the full server/ structure
- Fixed by:
  - FlowBridge: `cd /var/www/flowbridge.live && PORT=3847 pm2 start server/index.js --name flowbridge`
  - MetricPulse: `cd /var/www/metricpulse.live/html && PORT=3848 pm2 start server.js --name metricpulse`
  - SupportForge: `cd /var/www/supporthq.live/html && PORT=3846 pm2 start src/server.js --name supporthq`
- All 3 services now online on correct ports
- PM2 saved with `pm2 save`
- Note: services may need ecosystem.config.js files for proper startup on reboot

### ~09:24 UTC - Morning Check
- All services still up: flowbridge(3847), metricpulse(3848), supporthq(3846)
- PM2 startup saved
- VPS otherwise healthy (nginx, PostgreSQL, Redis all running)