# Daily Notes — 2026-05-14

## FitGardener.com — Built & Deployed

Dan bought **fitgardener.com** and wanted a personal site about him + gardening. Built on `gardenproject.3nd.dev`.

**Stack:** Express.js + EJS + JSON-driven content
**App dir:** `/var/www/gardenproject.3nd.dev/`
**Port:** 3841
**PM2 name:** gardenproject
**SSL:** Let's Encrypt (certbot)
**Content file:** `/var/www/gardenproject.3nd.dev/data/site.json` — edit this, site updates instantly

**Design:** Premium editorial feel — Playfair Display serif headings, Source Serif 4 body, forest green (#1a2e1a) + warm gold (#c8963e) accent, full-bleed Unsplash hero photography, animated card entrances, responsive mobile layout.

**Pages:** Home (hero + about + garden banner + journal preview), Garden Journal (all posts), Static pages (About, Tips, Contact), Blog post view.

**DNS:** fitgardener.com points to the same VPS. May need www redirect / CNAME setup if not already.

---

## QRForge (qr.3nd.dev) — Fixes Applied

- Login button click not firing — JS syntax error in template literal inside `innerHTML` in `loadList()`
- Fix: rewrote `loadList()` using `document.createElement('div')` + `innerHTML` (NOT template literals) to avoid JS parser issues with nested quotes
- JS button handlers use `onclick="functionName()"` HTML attributes (not addEventListener) for reliability
- Working conclusion: Dan confirmed login works and it was in mobile mode but zoomed out
- Fixed mobile responsive CSS for the QRForge UI (smaller text, proper inputs)
- QR type selector now properly shows URL/WiFi/vCard/SMS/Email/Text input fields based on selection

**DB:** `/var/www/111117.lab.3nd.dev/data/qrforge.db`
**PM2:** qrforge (port 3860)

---

## VPS Notes

- Gardenproject on port 3841 — check if port needs to be opened in firewall if inaccessible
- All *.3nd.dev subdomains route through the same nginx — gardenproject.3nd.dev should work if DNS propagates